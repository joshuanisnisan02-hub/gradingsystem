# Contributing

1. Create a focused feature branch.
2. Keep schema changes in a new Supabase migration.
3. Run `flutter analyze` and `flutter test` before opening a pull request.
4. Do not commit credentials, generated build output, or unrelated changes.
5. Add tests for grade calculations and every RLS-sensitive workflow.
